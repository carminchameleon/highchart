import React from "react";
import styled from "styled-components";

// export interface IEmployee {}
// ToDo : 1. 데이터 패치 : initialProps로 진행 -> page/api 만들어서 목업 데이터 불러올 수 있도록 하기
// ToDo : 2. 데이터를 가지고 와서, 데이터가 있을 때 Option을 정함
// ToDo : 3. 그 옵션이 있을 때 데이터를 뿌릴 수 있도록 하기
// TODO : 4. 근데 그 API 를 뿌릴 때  React.Query를 사용할 수 있도록 하기
const Employee: React.FC = ({}) => {
  return <div>임직원 수</div>;
};

export default Employee;
